// What will the following code log to the console and why?

console.log(a);

var a = 1;

// 'undefined' b/c variables declared using var are hoisted to the top of the program, so they are available but they are initialized to undefined until actually declared
