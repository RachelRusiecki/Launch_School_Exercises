// Define a variable employee using an object type annotation that includes properties id (number), name (string), and department (string), and initialize it with some default values.

// Expected outcome:

// An employee object with specified types for its properties.

const employee: { id: number, name: string, department: string } = {
  id: 1,
  name: 'Rachel',
  department: 'Software Engineering',
};

console.log(employee);
