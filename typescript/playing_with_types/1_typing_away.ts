// Create three variables, each with an explicit type annotation. Set them to a string, a number, and a boolean value.

// Expected outcome:

// A variable with a string value, annotated with type string.
// A variable with a number value, annotated with type number.
// A variable with a boolean value, annotated with type boolean.

const string: string = 'hello';
const number: number = 1;
const boolean: boolean = true;
