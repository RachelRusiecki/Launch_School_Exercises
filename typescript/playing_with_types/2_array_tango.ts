// Create an array called colors that can only hold strings. Initialize it with a few colors of your choice.

// Expected outcome:

// An array that allows only strings.

const colors: string[] = ['red', 'yellow', 'blue'];
